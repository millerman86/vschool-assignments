import React from "react";

function Home() {
  return <div></div>;
}

export default Home;

// Things I need:
// Header (at least to say what the purpose of the app is, or what the title is)
// Redirect to search.

// Pages
// Home (also is search page) -> Stats
// Home
// Home -> Stats (should the state be saved? )
